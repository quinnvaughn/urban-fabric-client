mutation Register($input: RegisterInput!) {
	register(input: $input) {
		... on User {
			...Me
		}
		... on ValidationError {
			errors {
				field
				message
			}
		}
		... on ConflictError {
			message
		}
	}
}
