mutation Login($input: LoginInput!) {
	login(input: $input) {
		... on ConflictError {
			message
		}
		... on UnauthorizedError {
			message
		}
		... on User {
			id
			name
			email
			role
		}
	}
}
