query ProposalCommentReplies($parentId: ID!) {
	proposalCommentReplies(parentId: $parentId) {
		...ReplyCard
	}
}

fragment ReplyCard on ProposalComment {
	...CommentCard
	replyToComment {
		id
		user {
			id
			name
			username
		}
	}
}
