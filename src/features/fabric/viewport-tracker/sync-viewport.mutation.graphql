mutation SyncViewport($input: SyncViewportInput!) {
	syncViewport(input: $input) {
		... on Fabric {
			id
		}
		... on ForbiddenError {
			message
		}
		... on NotFoundError {
			message
		}
		... on UnauthorizedError {
			message
		}
	}
}
