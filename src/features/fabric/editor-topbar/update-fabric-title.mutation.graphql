mutation UpdateFabricTitle($input: UpdateFabricTitleInput!) {
	updateFabricTitle(input: $input) {
		... on Fabric {
			id
			title
			updatedAt
		}
		... on NotFoundError {
			message
		}
		... on ForbiddenError {
			message
		}
		... on UnauthorizedError {
			message
		}
	}
}
