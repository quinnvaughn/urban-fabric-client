query MyFabrics($limit: Int, $offset: Int, $search: String) {
	myFabrics(limit: $limit, offset: $offset, search: $search) {
		... on MyFabricsPayload {
			total
			hasMore
			fabrics {
				id
				proposal {
					id
					isPublished
				}
				locationCity
				locationRegion
				thumbnail
				title
				updatedAt
			}
		}
		... on UnauthorizedError {
			message
		}
	}
}
