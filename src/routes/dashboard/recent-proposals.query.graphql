query RecentProposals($limit: Int) {
	myProposals(limit: $limit) {
		... on MyProposalsPayload {
			total
			proposals {
				id
				fabricId
				title
				slug
				isPublished
				snapshotLocationCity
				snapshotLocationRegion
				snapshotThumbnail
				updatedAt
				likeCount
				viewCount
				commentCount
			}
		}
		... on UnauthorizedError {
			message
		}
	}
}
