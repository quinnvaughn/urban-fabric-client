mutation EditEmail($input: EditEmailInput!) {
	editEmail(input: $input) {
		... on UnauthorizedError {
			message
		}
		... on NotFoundError {
			message
		}
		... on User {
			email
			id
		}
		... on ValidationError {
			errors {
				message
				field
			}
		}
	}
}
