mutation EditPassword($input: EditPasswordInput!) {
	editPassword(input: $input) {
		... on UnauthorizedError {
			message
		}
		... on NotFoundError {
			message
		}
		... on User {
			id
			hasPassword
		}
	}
}
