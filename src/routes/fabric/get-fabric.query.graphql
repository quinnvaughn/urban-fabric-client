query GetFabric($fabricId: ID!) {
	fabric(id: $fabricId) {
		... on Fabric {
			id
			title
			viewportBearing
			viewportCenter {
				lng
				lat
			}
			viewportPitch
			viewportZoom
		}
		... on NotFoundError {
			message
		}
	}
}
