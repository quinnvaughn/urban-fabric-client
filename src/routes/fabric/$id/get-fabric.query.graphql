query GetFabric($fabricId: ID!) {
	fabric(id: $fabricId) {
		... on Fabric {
			id
			title
			viewportBearing
			viewportCenter {
				lng
				lat
			}
			viewportZoom
			elements
		}
		... on NotFoundError {
			message
		}
	}
}
