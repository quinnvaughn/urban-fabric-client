query GetFabric($fabricId: ID!) {
	fabric(id: $fabricId) {
		... on Fabric {
			id
			title
			bearing
			center {
				lng
				lat
			}
			zoom
			elements
		}
		... on NotFoundError {
			message
		}
	}
}
