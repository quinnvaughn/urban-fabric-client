query GetFabric($fabricId: ID!) {
	fabric(id: $fabricId) {
		... on Fabric {
			id
			title
			center {
				lng
				lat
			}
			zoom
			elements
			thumbnail
		}
		... on NotFoundError {
			message
		}
	}
}
