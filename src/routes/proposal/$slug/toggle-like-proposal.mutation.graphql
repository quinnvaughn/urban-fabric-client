mutation ToggleProposalLike($input: ToggleProposalLikeInput!) {
	toggleProposalLike(input: $input) {
		... on UnauthorizedError {
			message
		}
		... on NotFoundError {
			message
		}
		... on Proposal {
			id
			likeCount
			isLikedByMe
		}
	}
}
